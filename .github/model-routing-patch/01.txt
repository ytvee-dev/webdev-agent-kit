diff --git a/.claude-plugin/plugin.json b/.claude-plugin/plugin.json
index 33b4759..a47203d 100644
--- a/.claude-plugin/plugin.json
+++ b/.claude-plugin/plugin.json
@@ -1,6 +1,6 @@
 {
   "name": "webdev-agent-kit",
-  "version": "0.5.0",
+  "version": "1.0.0",
   "description": "Portable frontend workflow skills for planning, implementation, review, and visual QA.",
   "author": {
     "name": "Yevgeniy Tyan"
diff --git a/.codex-plugin/plugin.json b/.codex-plugin/plugin.json
index 6c1e108..803236f 100644
--- a/.codex-plugin/plugin.json
+++ b/.codex-plugin/plugin.json
@@ -1,6 +1,6 @@
 {
   "name": "webdev-agent-kit",
-  "version": "0.5.0",
+  "version": "1.0.0",
   "description": "Frontend workflow skills for React and Next.js planning, implementation, review, and visual QA.",
   "skills": "./skills/"
 }
diff --git a/.github/workflows/quality-ci.yml b/.github/workflows/quality-ci.yml
index de391ae..db20afc 100644
--- a/.github/workflows/quality-ci.yml
+++ b/.github/workflows/quality-ci.yml
@@ -38,12 +38,12 @@ jobs:
       - name: Ruff lint
         run: |
           set -o pipefail
-          ruff check scripts skills/agent-rules-skill-author/scripts 2>&1 | tee ruff-lint.log
+          ruff check scripts skills/agent-rules-skill-author/scripts skills/project-onboarding-adapter/scripts 2>&1 | tee ruff-lint.log
 
       - name: Ruff format check
         run: |
           set -o pipefail
-          ruff format --diff scripts skills/agent-rules-skill-author/scripts 2>&1 | tee ruff-format.log
+          ruff format --diff scripts skills/agent-rules-skill-author/scripts skills/project-onboarding-adapter/scripts 2>&1 | tee ruff-format.log
 
       - name: YAML lint
         run: |
diff --git a/.github/workflows/release.yml b/.github/workflows/release.yml
index 19bcc2c..7652bce 100644
--- a/.github/workflows/release.yml
+++ b/.github/workflows/release.yml
@@ -4,22 +4,32 @@ on:
   push:
     tags:
       - 'v*.*.*'
+  workflow_call:
+    inputs:
+      release_tag:
+        description: Existing validated release tag to publish
+        required: true
+        type: string
 
 permissions:
   contents: write
 
 concurrency:
-  group: release-${{ github.ref }}
+  group: release-${{ inputs.release_tag || github.ref }}
   cancel-in-progress: false
 
 jobs:
   release:
     name: Build and publish portable targets
     runs-on: ubuntu-latest
+    env:
+      RELEASE_TAG: ${{ inputs.release_tag || github.ref_name }}
 
     steps:
       - name: Checkout
         uses: actions/checkout@v4
+        with:
+          ref: ${{ inputs.release_tag || github.ref }}
 
       - name: Set up Python
         uses: actions/setup-python@v5
@@ -30,6 +40,7 @@ jobs:
         run: |
           python -m py_compile scripts/*.py
           python -m py_compile skills/agent-rules-skill-author/scripts/*.py
+          python -m py_compile skills/project-onboarding-adapter/scripts/*.py
 
       - name: Validate schemas
         run: python scripts/validate_schemas.py --strict-graph
@@ -42,10 +53,10 @@ jobs:
 
       - name: Validate release tag and installation guides
         run: |
-          python scripts/validate_release_tag.py --tag "${GITHUB_REF_NAME}"
+          python scripts/validate_release_tag.py --tag "${RELEASE_TAG}"
           python scripts/validate_install_guides.py
           python scripts/build_release_notes.py \
-            --tag "${GITHUB_REF_NAME}" \
+            --tag "${RELEASE_TAG}" \
             --check \
             --require-content
 
@@ -79,6 +90,9 @@ jobs:
       - name: Validate skill evals
         run: python scripts/validate_skill_evals.py
 
+      - name: Validate GPT role setup and routing contracts
+        run: python scripts/validate_model_routing.py
+
       - name: Build portable targets
         run: python scripts/build_skill_targets.py
 
@@ -159,15 +173,15 @@ jobs:
         run: |
           mkdir -p release-artifacts
           python scripts/build_release_notes.py \
-            --tag "${GITHUB_REF_NAME}" \
+            --tag "${RELEASE_TAG}" \
             --output release-artifacts/RELEASE_NOTES.md \
             --require-content
           python scripts/build_release_archives.py \
             --output release-artifacts \
-            --version "${GITHUB_REF_NAME}"
+            --version "${RELEASE_TAG}"
           python scripts/validate_release_archive.py \
             --directory release-artifacts \
-            --version "${GITHUB_REF_NAME}"
+            --version "${RELEASE_TAG}"
 
           echo 'Release artifacts:'
           find release-artifacts -maxdepth 1 -type f -print | sort
@@ -175,7 +189,7 @@ jobs:
       - name: Upload release artifact bundle
         uses: actions/upload-artifact@v4
         with:
-          name: webdev-agent-kit-release-${{ github.ref_name }}
+          name: webdev-agent-kit-release-${{ env.RELEASE_TAG }}
           path: release-artifacts/
 
       - name: Publish GitHub release
@@ -183,11 +197,11 @@ jobs:
           GH_TOKEN: ${{ github.token }}
         run: |
           files=(
-            "release-artifacts/webdev-agent-kit-codex-${GITHUB_REF_NAME}.tar.gz"
-            "release-artifacts/webdev-agent-kit-claude-code-${GITHUB_REF_NAME}.tar.gz"
-            "release-artifacts/webdev-agent-kit-cursor-${GITHUB_REF_NAME}.tar.gz"
-            "release-artifacts/webdev-agent-kit-vs-code-codex-${GITHUB_REF_NAME}.tar.gz"
-            "release-artifacts/webdev-agent-kit-vs-code-claude-${GITHUB_REF_NAME}.tar.gz"
+            "release-artifacts/webdev-agent-kit-codex-${RELEASE_TAG}.tar.gz"
+            "release-artifacts/webdev-agent-kit-claude-code-${RELEASE_TAG}.tar.gz"
+            "release-artifacts/webdev-agent-kit-cursor-${RELEASE_TAG}.tar.gz"
+            "release-artifacts/webdev-agent-kit-vs-code-codex-${RELEASE_TAG}.tar.gz"
+            "release-artifacts/webdev-agent-kit-vs-code-claude-${RELEASE_TAG}.tar.gz"
             "release-artifacts/webdev-agent-kit-codex.tar.gz"
             "release-artifacts/webdev-agent-kit-claude-code.tar.gz"
             "release-artifacts/webdev-agent-kit-cursor.tar.gz"
@@ -196,14 +210,14 @@ jobs:
             "release-artifacts/SHA256SUMS"
           )
 
-          if gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
-            echo "Release ${GITHUB_REF_NAME} already exists. Uploading assets with --clobber."
-            gh release upload "${GITHUB_REF_NAME}" "${files[@]}" --clobber
+          if gh release view "${RELEASE_TAG}" >/dev/null 2>&1; then
+            echo "Release ${RELEASE_TAG} already exists. Uploading assets with --clobber."
+            gh release upload "${RELEASE_TAG}" "${files[@]}" --clobber
           else
-            echo "Release ${GITHUB_REF_NAME} does not exist. Creating release."
-            gh release create "${GITHUB_REF_NAME}" \
+            echo "Release ${RELEASE_TAG} does not exist. Creating release."
+            gh release create "${RELEASE_TAG}" \
               "${files[@]}" \
-              --title "WebDev Agent Kit ${GITHUB_REF_NAME}" \
+              --title "WebDev Agent Kit ${RELEASE_TAG}" \
               --notes-file release-artifacts/RELEASE_NOTES.md \
               --verify-tag
           fi
diff --git a/.github/workflows/skill-pack-ci.yml b/.github/workflows/skill-pack-ci.yml
index dcb2ef7..d406e40 100644
--- a/.github/workflows/skill-pack-ci.yml
+++ b/.github/workflows/skill-pack-ci.yml
@@ -27,6 +27,7 @@ jobs:
         run: |
           python -m py_compile scripts/*.py
           python -m py_compile skills/agent-rules-skill-author/scripts/*.py
+          python -m py_compile skills/project-onboarding-adapter/scripts/*.py
 
       - name: Validate schemas
         id: validate_schemas
@@ -145,6 +146,9 @@ jobs:
         if: steps.validate_evals.outputs.status != '0'
         run: exit ${{ steps.validate_evals.outputs.status }}
 
+      - name: Validate GPT role setup and routing contracts
+        run: python scripts/validate_model_routing.py
+
       - name: Build portable targets
         run: python scripts/build_skill_targets.py
 
