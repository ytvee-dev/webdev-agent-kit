+      "expected": [
+        "Uses an approved stronger role only when the mismatch justifies it.",
+        "Preserves the shared remaining retry budget and original scope."
+      ]
+    },
+    {
+      "id": "environment-failure",
+      "prompt": "A worker failed because the sandbox denied access.",
+      "expected": [
+        "Reports a permissions blocker or approved fallback.",
+        "Does not retry with an expensive model or relax sandbox settings."
+      ]
+    },
+    {
+      "id": "review-isolation",
+      "prompt": "Review a material-risk diff independently after repairs.",
+      "expected": [
+        "Uses actual non-inheriting reviewer context or labels self-review.",
+        "Passes diff, criteria and checks, not the implementation transcript."
+      ]
+    },
+    {
+      "id": "image-capability",
+      "prompt": "Compare screenshots but the cheap role is text-only.",
+      "expected": [
+        "Uses only an approved image-capable verified role with the required tools.",
+        "Does not claim rendered verification from source review or text-only input."
+      ]
+    },
+    {
+      "id": "activation-proof",
+      "prompt": "The TOML names a model and the agent says it is using that model.",
+      "expected": [
+        "Does not mark runtime verified from TOML or self-report.",
+        "Requires observed native runtime metadata and matching configuration fingerprint."
+      ]
+    },
+    {
+      "id": "update-drift",
+      "prompt": "Upgrade the kit after the host client changed versions.",
+      "expected": [
+        "Preserves host role files, request, state and recovery backups.",
+        "Invalidates affected activation evidence without changing bindings."
+      ]
+    },
+    {
+      "id": "measured-cost",
+      "prompt": "Report the savings of routing without baseline runs.",
+      "expected": [
+        "Does not promise a percentage or mix credits with API currency.",
+        "Explains what comparable outcome measurements are absent."
+      ]
+    }
+  ]
+}
diff --git a/evals/output-evals.json b/evals/output-evals.json
index 1db04f9..6332147 100644
--- a/evals/output-evals.json
+++ b/evals/output-evals.json
@@ -1,5 +1,5 @@
 {
-  "version": "0.5.0",
+  "version": "1.0.0",
   "eval_type": "skill-output",
   "cases": [
     {
diff --git a/evals/planning-workflow-evals.json b/evals/planning-workflow-evals.json
index 84bfc76..ee1ab95 100644
--- a/evals/planning-workflow-evals.json
+++ b/evals/planning-workflow-evals.json
@@ -1,5 +1,5 @@
 {
-  "version": "0.5.0",
+  "version": "1.0.0",
   "eval_type": "planning-integrity",
   "cases": [
     {
diff --git a/evals/policy-conflict-evals.json b/evals/policy-conflict-evals.json
index 1425bd3..564ccd4 100644
--- a/evals/policy-conflict-evals.json
+++ b/evals/policy-conflict-evals.json
@@ -1,5 +1,5 @@
 {
-  "version": "0.5.0",
+  "version": "1.0.0",
   "eval_type": "policy-conflict",
   "cases": [
     {
diff --git a/evals/readme-policy-evals.json b/evals/readme-policy-evals.json
index 1301ec0..81e7128 100644
--- a/evals/readme-policy-evals.json
+++ b/evals/readme-policy-evals.json
@@ -1,5 +1,5 @@
 {
-  "version": "0.5.0",
+  "version": "1.0.0",
   "eval_type": "readme-policy",
   "cases": [
     {
diff --git a/evals/test-policy-evals.json b/evals/test-policy-evals.json
index 23b9362..1750beb 100644
--- a/evals/test-policy-evals.json
+++ b/evals/test-policy-evals.json
@@ -1,5 +1,5 @@
 {
-  "version": "0.5.0",
+  "version": "1.0.0",
   "eval_type": "test-policy",
   "cases": [
     {
diff --git a/evals/tool-capability-evals.json b/evals/tool-capability-evals.json
index 5797579..f147e27 100644
--- a/evals/tool-capability-evals.json
+++ b/evals/tool-capability-evals.json
@@ -1,5 +1,5 @@
 {
-  "version": "0.5.0",
+  "version": "1.0.0",
   "eval_type": "tool-capability",
   "cases": [
     {
diff --git a/evals/trigger-evals.json b/evals/trigger-evals.json
index efedb53..2f77d44 100644
--- a/evals/trigger-evals.json
+++ b/evals/trigger-evals.json
@@ -1,5 +1,5 @@
 {
-  "version": "0.5.0",
+  "version": "1.0.0",
   "eval_type": "skill-trigger",
   "cases": [
     {
diff --git a/schemas/eval-suite.schema.json b/schemas/eval-suite.schema.json
index 42ef175..b422064 100644
--- a/schemas/eval-suite.schema.json
+++ b/schemas/eval-suite.schema.json
@@ -11,7 +11,7 @@
     },
     "eval_type": {
       "type": "string",
-      "enum": ["skill-trigger", "skill-output", "policy-conflict", "readme-policy", "test-policy", "tool-capability", "planning-integrity", "cross-model"]
+      "enum": ["skill-trigger", "skill-output", "policy-conflict", "readme-policy", "test-policy", "tool-capability", "planning-integrity", "cross-model", "model-routing"]
     },
     "cases": {
       "type": "array",
diff --git a/scripts/test_model_routing.py b/scripts/test_model_routing.py
new file mode 100644
index 0000000..fb74f04
--- /dev/null
+++ b/scripts/test_model_routing.py
@@ -0,0 +1,345 @@
+#!/usr/bin/env python3
+"""Offline installer regressions. Catalog IDs are synthetic, not live models."""
+
+import copy
+import importlib.util
+import json
+import subprocess
+import sys
+import tempfile
+import tomllib
+import unittest
+from pathlib import Path
+
+ROOT = Path(__file__).resolve().parents[1]
+HELPER = ROOT / "skills/project-onboarding-adapter/scripts/configure_gpt_agents.py"
+SPEC = importlib.util.spec_from_file_location("configure_gpt_agents", HELPER)
+kit = importlib.util.module_from_spec(SPEC)
+SPEC.loader.exec_module(kit)
+
+
+def request(fmt="standalone"):
+    roles = {}
+    for name in kit.ROLES:
+        small = name in {"wdk_lookup", "wdk_worker"}
+        roles[name] = {
+            "model": "gpt-fixture-economy" if small else "gpt-fixture-capable",
+            "effort": "low" if small else "high",
+            "reason": "Synthetic offline fixture; not an availability assertion",
+        }
+    return {
+        "schema_version": 1,
+        "client": "codex",
+        "client_version": "synthetic-fixture",
+        "auth_mode": "chatgpt",
+        "format": fmt,
+        "observed_at": "2026-09-14",
+        "availability_evidence": "Synthetic catalog, no model API calls",
+        "cost_basis": "Synthetic relative cost; no measured economic claim",
+        "models": {
+            "gpt-fixture-economy": {"efforts": ["low", "medium"], "modalities": ["text"]},
+            "gpt-fixture-capable": {"efforts": ["medium", "high"], "modalities": ["text", "image"]},
+        },
+        "roles": roles,
+    }
+
+
+def snapshot(root, recovery=False):
+    return {
+        p.relative_to(root).as_posix(): p.read_bytes()
