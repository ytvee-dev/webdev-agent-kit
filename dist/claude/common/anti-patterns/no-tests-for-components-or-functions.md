# No Tests For Components Or Functions

Do not create tests for components or functions.

Do not add test files, test scripts, test dependencies, or test framework setup.

Use existing lint, typecheck, build, rendered UI verification, and manual smoke checks instead.
