interface:
    display_name: 'Project Context Adapter'
    short_description: 'Refresh frontend project overlays'
    default_prompt: 'Use $project-context-adapter to refresh project overlays and frontend path indexes.'
policy:
    allow_implicit_invocation: true
dependencies:
    tools:
        - type: 'mcp'
          value: 'filesystem_server'
          description: 'Read local project files and update project overlay facts.'
