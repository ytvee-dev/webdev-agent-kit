interface:
    display_name: 'React State Workflow'
    short_description: 'Shared React state, context, Redux, and selectors'
    default_prompt: 'Use $react-state-workflow for context, Redux, selectors, or store-like shared-state work.'
policy:
    allow_implicit_invocation: true
