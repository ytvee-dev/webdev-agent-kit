interface:
    display_name: 'Execution Plan Manager'
    short_description: 'Plan frontend work slices'
    default_prompt: 'Use $execution-plan-manager to split standard or deep frontend work into traceable verified slices with context budget, checkpoint rules, and stop/resume state.'
policy:
    allow_implicit_invocation: true
