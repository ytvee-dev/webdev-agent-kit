interface:
    display_name: 'Execution Plan Manager'
    short_description: 'Create, resume, or analyze plans'
    default_prompt: 'Use $execution-plan-manager to create, resume, or read-only analyze a standard or deep frontend execution plan with traceable slices and verification.'
policy:
    allow_implicit_invocation: true
