interface:
    display_name: 'Architecture From Spec'
    short_description: 'Turn a user specification into source-backed architecture guidance'
    default_prompt: 'Use $architecture-from-spec when the user provides a spec or large refactor brief and wants architecture guidance.'
policy:
    allow_implicit_invocation: true
