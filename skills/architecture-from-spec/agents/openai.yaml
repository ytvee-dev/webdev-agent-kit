interface:
    display_name: 'Architecture From Spec'
    short_description: 'React architecture from specs and refactor briefs'
    default_prompt: 'Use $architecture-from-spec when the user provides a React/frontend spec or large refactor brief.'
policy:
    allow_implicit_invocation: true
