interface:
  display_name: "Frontend Quality Reviewer"
  short_description: "Cross-stack frontend quality review"
  default_prompt: "Use $frontend-quality-reviewer to review this frontend work. In non-target frontend projects, ignore React-only checks and review only evidence that fits the project."

policy:
  allow_implicit_invocation: true
